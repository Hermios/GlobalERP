<Window x:Class="GlobalERP.Editor.CreateModelMessageBox.CreateModelMessageBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GlobalERP.Editor.CreateModelMessageBox"
        xmlns:ucmb="clr-namespace:StandardTools.ViewHandler.UserControlButtons;assembly=StandardTools"
        mc:Ignorable="d"
        Title="{Binding Title}" SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="{Binding Text}"/>
        <ComboBox Grid.Row="1" ItemsSource="{Binding VMTypes}"/>
        <ucmb:UserControlButtonsView Grid.Row="2" DataContext="{Binding UCButtonsVM}"/>
    </Grid>
</Window>
